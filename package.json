{
  "name": "hellonado-site",
  "private": true,
  "scripts": {
    "start": "node tools/serve.js",
    "test": "node --check script.js && node --check js/submission-guard.js && node --test tests/*.test.js"
  }
}
