# 새 메인 화면 안전 배포 계획

## 현재 상태

- 현재 운영 중인 `hellonado.com`은 그대로 유지합니다.
- 이 폴더는 로컬 검토용 v11 사본이며 운영 저장소와 분리되어 있습니다.
- 로컬에서는 2026-09-15 샘플 선생님·시간 데이터로 화면과 신청 과정을 시험합니다.
- 운영 환경에서는 최신 확인 전 화면 확인용 시간의 신청 버튼이 자동으로 비활성화됩니다.
- 실시간 목록용 공개 조회 함수와 알림 채널은 아직 운영 Supabase에 연결하지 않았습니다. 따라서 지금 파일만 바로 배포하면 실시간 목록이 아니라 안전한 샘플 안내 화면이 보입니다.
- 메인은 현재 운영 중인 첫 화면 구조로 되돌리고, 일반 신청 CTA에만 `바로 신청하기 / 선생님 둘러보고 신청하기` 선택창을 추가했습니다.
- 정규/체험·지역 선택과 선생님 목록은 `teachers.html` 상단으로 이동했습니다. 기존 시간대 직접 신청 링크의 검증 파라미터는 그대로 유지합니다.
- 선생님 회전 영역은 현재 공개 목록 데이터만 사용합니다. 제외된 과거 선생님 하드코딩 목록은 사용하지 않습니다.
- 가능한 언어와 영어·한국어 소개 영상은 새 공개 조회 v3에서만 추가합니다. 승인된 영상이 없으면 학생 화면에 영상 영역 자체가 나타나지 않습니다.
- 가능 시간 선택은 즉시 예약 확정이 아니라 해당 가능 시간대 신청입니다. 정확한 시작 시간과 장소는 선생님 확인 후 확정합니다.

## 권장 배포 순서

1. **이전 버전 확정**  
   배포 직전 운영 `main`의 커밋을 기록하고 `v1-before-homepage-finder` 태그와 ZIP을 남깁니다.

2. **기존 공개 조회 v2 확인**  
   기존 공개 함수는 유지하고 `supabase/public-teacher-directory-rpc-v2.sql`의 `get_public_teacher_directory_v2` 결과가 현재 운영 일정과 일치하는지 먼저 확인합니다. Oscar의 실제 고정 ID를 확인할 수 있다면 배포 전에 이름 기반 임시 예외를 그 ID 기준으로 바꿉니다.

   교사 카탈로그의 `plan_groups`도 Oscar 외 현재 선생님은 `["economy", "standard"]`, Oscar는 `["standard", "premium"]`으로 채웁니다. 수동 맞춤 매칭에서 사용하는 기존 `get_available_teachers(p_plan)`도 같은 복수 수업 방식 정책으로 업데이트한 뒤 결과를 점검합니다.

   직접 선택 신청은 Jotform으로 보내기 직전에 Vercel API가 같은 공개 조회 결과를 다시 확인하도록 구성되어 있습니다. 프론트 전환 전에 Vercel에 `SUPABASE_URL`과 `SUPABASE_ANON_KEY`(또는 `SUPABASE_PUBLISHABLE_KEY`)를 설정하고 재배포해야 합니다. 기존 `JOTFORM_API_KEY`도 그대로 유지합니다.

   기존 페이지 캐시에는 선생님 선택 메타데이터를 브라우저에서 지우는 v1 스크립트가 남아 있을 수 있습니다. API를 먼저 배포할 때 `ALLOW_LEGACY_SELECTED_PAYLOADS=true`를 명시해 이 구버전 신청만 임시 허용합니다. 새 API는 수동 신청으로 들어온 teacher ID·이름을 제거하므로, 신청 유형만 바꿔 선생님 선택을 남기는 단순 우회는 차단합니다.

3. **언어·영상 제출 구조를 별도 적용**  
   선생님 포털의 `sql/teacher_profile_media_update.sql`을 개발 브랜치에서 먼저 검토·적용합니다. 학생 사이트의 `supabase/teacher-profile-media-schema-review.sql`은 같은 내용을 담은 검토용 복사본이므로 **둘 중 하나만 한 번 실행**합니다. 이 SQL로 가능한 언어 테이블, 비공개 영상 제출 버킷, 영어/한국어 영상별 검수 상태를 준비합니다. 선생님은 본인 제출 파일만 읽고 쓸 수 있어야 하고, 승인 상태·공개 경로는 운영자만 변경할 수 있어야 합니다. 승인 전 파일은 익명 사용자에게 공개되지 않아야 합니다.

   제출 영상 검수에는 선생님 포털 관리자 화면의 10분짜리 비공개 미리보기를 사용하고, 승인·반려는 `sql/teacher_profile_media_review.sql`의 신뢰된 SQL 절차로 처리합니다. 승인된 파일은 `teacher-intro-videos-public` 버킷에 MP4 또는 WEBM으로 복사한 뒤 공개 경로만 기록합니다. 그다음 학생 사이트의 `supabase/public-teacher-directory-rpc-v3.sql`을 적용합니다. 기존 v2는 바꾸지 않으며, v3는 가능한 언어와 승인된 영어·한국어 영상만 반환합니다. 익명 사용자가 전화번호·이메일·카카오톡 ID·계좌·비공개 제출 경로를 읽을 수 없는지 확인합니다. 프론트는 v3 함수가 아직 없는 경우에만 v2로 복귀하며, v2의 기존 영상 필드는 항상 버립니다.

4. **실시간 갱신 연결**  
   선생님이 가능 시간을 저장한 뒤 payload가 없는 `directory-changed` Broadcast만 보내도록 연결합니다. 홈페이지는 이 신호를 받으면 공개 조회 함수를 다시 호출합니다. 운영 연결이 검증된 뒤에만 `NADO_MEMBER_CONFIG.TEACHER_DIRECTORY_REALTIME_CHANNEL` 값을 설정합니다.

5. **비공개 최종 점검**  
   운영과 분리된 미리보기에서 송도/서울, 정규 수업, 송도 무료 체험, 1회 유료 체험, 선택 카드 접기, 정규 신청의 Economy/Standard 재선택, 선생님 상세, 1시간/2시간 제한, Premium 상담 완료 화면을 모바일과 데스크톱으로 확인합니다. 초등학생 이하 선택 시 Standard만 보이고 무료 체험·Economy·Premium 신청이 차단되는지도 확인합니다. 이때 신청은 `?test=1` 모드로만 진행해 실제 Jotform 접수를 만들지 않습니다.

6. **프론트엔드 전환**  
   GA4에서 최근 4주간 신청이 가장 적은 시간을 확인해 배포합니다. 별도 데이터가 없다면 **화요일 또는 수요일 밤 11시(KST)** 를 기본 창으로 권장합니다. 최신 `main`에서 새 release 브랜치를 만든 뒤 검토·승인된 변경만 PR로 합칩니다.

7. **배포 후 60분 관찰**  
   실제 브라우저에서 송도와 서울 목록을 각각 열고, Vercel 신청 API 오류, Jotform 신규 접수, GA4 신청 오류 이벤트, Supabase 공개 조회 오류를 확인합니다.

8. **문제 시 즉시 복귀**  
   새 프론트 merge를 `git revert`해 이전 페이지로 되돌립니다. GitHub Pages 캐시 때문에 방문자에게 완전히 전파되기까지 약 10분을 잡습니다. 먼저 추가한 Supabase 공개 함수는 기존 사이트에 영향을 주지 않으므로 긴급 상황에서 바로 삭제하지 않습니다.

9. **구버전 호환 창 닫기**  
   새 프론트 배포 후 24시간 동안 기존 신청과 새 신청 오류를 확인합니다. 문제가 없고 GitHub Pages 캐시 및 배포 전에 열어 둔 신청 탭의 사용 시간이 충분히 지났으면 Vercel에서 `ALLOW_LEGACY_SELECTED_PAYLOADS=false`로 바꾸고 API를 재배포합니다. 이후 메타데이터가 제거된 구버전 선생님 선택 요청은 409로 거부됩니다. 이 단계를 생략하면 임시 호환 경로가 계속 남으므로 배포 완료 조건에 포함합니다.

## 배포 승인 조건

- 송도/서울 선택이 각각 정확한 선생님만 보여줌
- 현재 승인 데이터에 따라 일반 카드에는 Economy·Standard가 표시되고, Premium은 승인된 담당 선생님 카드에만 표시됨
- Economy/Standard 가능 시간에는 `신청하기` 하나만 보이고, 신청 화면에서 실제 가능한 두 방식만 다시 선택할 수 있음
- 흰색 시간대 칸 전체가 신청 링크이며 작은 화면에서도 가로 넘침이 없음
- 송도 지역 선택 카드에는 모든 요금제의 지정 장소 월 1만원 할인이 표시되고 체험 모드에서는 숨겨짐
- 같은 요일은 박스 하나에 묶이고, 선생님 전체 시간대가 4개 이상이면 요일과 관계없이 3개 뒤로 접힘
- 목록 전체 외곽 박스 없이 제목·설명·지역 탭만 하나의 박스로 표시됨
- 메인 선생님 회전 카드가 기존 첫 화면 안에서 자연스럽게 이어지고 현재 공개 목록만 사용함
- 메인 첫 화면이 기존 문구·일반 신청·체험 신청·회전 선생님 카드 구조로 보임
- 일반 신청 CTA에서만 두 경로 선택창이 열리고, `바로 신청하기`는 쿼리 없는 기존 `apply.html`로 이동함
- `선생님 둘러보고 신청하기`는 `teachers.html` 상단의 정규/체험·지역 선택 화면으로 이동함
- 체험 CTA와 선생님 시간대 직접 신청 링크는 일반 신청 선택창에 의해 가로채지지 않음
- `teachers.html` 상단에 정규/체험 선택과 지역 선택이 있고 중복 ID나 이중 이벤트 연결이 없음
- 송도·서울 지역 버튼이 데스크톱과 모바일 모두 한 줄에 나란히 표시됨
- 전반적인 본문·선택지 글씨가 확대되고 불필요한 바깥 여백이 줄어듦
- 선생님 목록은 데스크톱 2열·태블릿과 모바일 1열이며, 카드 사진이 확대되고 한줄 소개는 표시되지 않음
- `자세한 소개 보기`가 카드 상단에 있고 전체 소개는 상세 화면에 유지됨
- 상세 화면은 큰 프로필 사진이 중앙 상단에 표시됨
- 선생님이 자기소개 길이의 내림차순으로 정렬되고, 동률은 이름순임
- 최종 선택한 수업 방식이 신청 요약과 Jotform에 정확히 저장됨
- 체험 종류·플랜 선택이 끝나면 큰 카드는 접히고 선택 기록과 `변경`만 남은 뒤 선생님 목록으로 이어짐
- 체험 선생님 상세에는 `다른 체험 일정 상담하기` 버튼이 표시되지 않음
- `다시 선택`이 정규·체험 모두 선택했던 지역과 수업 조건이 유지된 선생님 목록으로 돌아감
- 무료 체험은 송도 Economy 및 IGC·트리플스트리트만 보여줌
- 초등학생 이하는 Standard 정규 또는 Standard 1회 유료 체험만 가능하며 무료 체험·Economy·Premium 제출은 서버에서도 거절됨
- 유료 체험은 Economy 35,000원, Standard 45,000원, Premium 55,000원으로 서울·인천에서 선택 가능
- Premium 정규·체험은 일반 결제 신청이 아니라 상담 요청으로 저장되고 4단계 확인 절차가 표시됨
- 선생님 카드 클릭 시 `진행 가능한 수업` 상자 없이 소개·언어·지역·시간이 표시되고, 승인 영상이 있을 때만 영상 영역이 추가됨
- 등록된 가능한 언어만 카드와 상세 화면에 표시되고 미등록 상태를 임의로 추정하지 않음
- 승인된 영상이 없으면 영상 영역이 전혀 없고, 한 언어만 있으면 단일 플레이어, 두 언어 모두 있으면 `English / 한국어` 전환이 표시됨
- 영상은 자동 재생되지 않고 YouTube·Vimeo·승인된 공개 Storage 경로 외 URL은 재생하지 않음
- 선생님 포털에서 영어·한국어 영상을 독립적으로 제출·교체·삭제할 수 있으며 검수 상태가 분리됨
- 60분 미만 시간은 신청할 수 없고, 120분 미만 시간은 2시간 선택이 비활성화됨
- 주 2회 선택 시 첫 번째 수업 시간대만 먼저 신청하고 두 번째 시간은 확정 시 조율한다는 안내가 표시됨
- 이전 답변은 `수정` 글자 없이 누를 수 있는 중앙 답변 칩으로 정리되고 다음 질문 카드가 자동으로 화면 중앙에 위치함
- 정규 신청 순서가 나이 → 수업 방식·시간 → 성별 → 영어 수준 → 목표 → 희망 날짜 → 추가사항 → 유입 경로 → 연락처 → 결제 안내로 진행됨
- 선택한 선생님 ID·시간·장소가 Jotform에 정확히 저장됨
- 오래되거나 주소를 조작한 선생님·시간 신청은 Jotform 전송 전에 거부됨
- 직접 선택의 희망 날짜가 실제 날짜이고 선택한 요일과 같으며 KST 기준 오늘 이후임
- 서버 검증 전에는 선택 메타데이터가 유지되고, 검증 후 Jotform에는 신청자가 쓴 전달사항만 저장됨
- 화면 확인용 데이터로는 운영 신청이 불가능하고, 화면에는 샘플·테스트 문구가 노출되지 않음
- 기존 맞춤 매칭 신청과 체험수업 직접 링크도 정상 작동함
- 프론트 배포 24시간 뒤 `ALLOW_LEGACY_SELECTED_PAYLOADS=false` 전환까지 완료됨

하나라도 실패하면 운영 배포를 미루고 현재 사이트를 계속 사용합니다.


## 2026-09-21 서울 장소 필터·유료 체험 경로
- 서울 세부 장소 필터는 공개 선생님의 실제 가능 장소를 기준으로 생성합니다.
- 무료 체험은 기존 매칭 절차를 유지합니다. 유료 체험의 다음 버튼은 신청 방법 선택창을 열며, 선생님을 선택해도 paid/trial 모드를 유지합니다.
- 커리큘럼의 세 플랜 카드는 login.html로 연결합니다. 홈 섹션 순서는 2026-09-21 사용자 승인 후 변경했습니다.
- 운영 DB에 get_public_teacher_directory_v2를 적용했습니다. 기존 active + accepting_students + active account 조건을 유지합니다. 추가 선생님의 공개 설정은 변경하지 않았습니다.
- 현재 티처스의 `IGC & 트스` + 세부 장소 구조를 각각의 지정장소로 변환합니다. 기타 인천 지역은 포함하지 않습니다.
- 익명 권한으로 서울 6명·송도 12명의 조회를 확인했습니다(중복 포함, 고유 12명).
- 코드 테스트 97개 통과. 실제 브라우저 화면 및 실신청 전송은 검증하지 않았습니다.
- 보안 점검의 공개 SECURITY DEFINER 알림은 이 공개 읽기 API의 의도된 권한입니다. search_path 고정, 허용 필드만 반환, 직접 테이블 권한 추가 없음. 기존 catalog 뷰 관련 알림은 기존 구성으로 별도 검토 대상입니다: [Supabase 안내](https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view).

- 홈 순서: 메인·동적 카드 → 신뢰 지표 → 수업료 → 차별점·특별한 이유 → 커리큘럼 → 왕초보 → 이용 방법 → 이야기 → FAQ → 신청 버튼. 섹션 내용·개수·링크 유지 및 순서 검증 완료.
